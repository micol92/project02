_schema-version: "3.2"
ID: project2
description: Generated by Fiori Tools
version: 0.0.1
modules:
- name: project2-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: project2-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - project2.zip
      name: project2
      target-path: resources/
- name: project2
  type: html5
  path: .
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
- name: project2-approuter
  type: approuter.nodejs
  path: project2-approuter
  requires:
  - name: project2_html_repo_runtime
  - name: uaa_project2
  - name: project2-destination-service
  parameters:
    disk-quota: 256M
    memory: 256M
- name: project2_ui_deployer
  type: com.sap.application.content
  path: .
  requires:
  - name: project2_html_repo_host
    parameters:
      content-target: true
resources:
- name: project2-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: project2-html5-srv
    service-plan: app-host
- name: project2-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: false
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: project2-destination-service
    service-plan: lite
- name: project2_html_repo_runtime
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-plan: app-runtime
- name: uaa_project2
  type: org.cloudfoundry.managed-service
  parameters:
    path: ./xs-security.json
    service: xsuaa
    service-name: project2-xsuaa-service
    service-plan: application
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
